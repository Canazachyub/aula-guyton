# Paquetes de skills descargados (.zip), ajenos al aula virtual. Se conservan
# en disco pero NO se versionan ni se publican en el repo de GitHub.
docs/SKILLS/

# Excel de simulacros UNA Puno: contiene DATOS PERSONALES REALES (DNI, correos,
# celulares de 231 personas) y el banco de preguntas (material de la academia).
# NUNCA debe entrar al repo público. Se usa solo localmente para generar el banco.
UNA PUNO SIMULACROS*.xlsx
BANQUEO_GUYTON.xlsx


# Datos de grabaciones para pegar al Sheet (no van al repo)
GRABACIONES_YOUTUBE.xlsx
GRABACIONES_IMPORT.xlsx
ACCESOS_DEMO.xlsx
PROMPTS_CURSOS.txt
PROMPTS_BANQUEO.txt

# Imagenes fuente (ChatGPT, sin optimizar) — las usadas ya estan optimizadas en web/public
imagenes/
~$*.xlsx
